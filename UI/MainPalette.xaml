<UserControl x:Class="ShipAutoCadPlugin.UI.MainPalette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ShipAutoCadPlugin.UI"
             Background="#FFF0F0F0" FontFamily="Segoe UI" FontSize="11">
    
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="MACGREGOR CAD TOOLS" FontWeight="Bold" FontSize="13" VerticalAlignment="Center" Foreground="#FF0055A5" Margin="5,0,0,0"/>
            <Button Grid.Column="1" Name="BtnHelp" Content=" ?" Click="BtnHelp_Click" Width="22" Height="22" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
        </Grid>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabItem Header="PANEL STRUCTURE">
                <local:PanelStructureView/>
            </TabItem>
            <TabItem Header="FITTING TOOLS">
                <local:FittingToolsView/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>